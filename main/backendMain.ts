import { Experiment } from './models/Experiment/Experiment'

// This is the main process for the backend, it is run inside the background.ts on startup.
export const main = async () => {
  const _test = new Experiment()
}
